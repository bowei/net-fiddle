import { Cable } from 'lucide-react';
import type { SidebarTemplate } from './vethPair';

export const netkitPair: SidebarTemplate = {
  type: 'netkit',
  label: 'netkit',
  color: '#0d9488',
  icon: Cable,
};
